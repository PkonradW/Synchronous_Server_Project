import java.net.Socket;

/**
 * A single instance of a client that connects to the server
 */
public class Client {
    Socket sock;
    int arg2;

    public Client(Socket sock, int arg2) {

    }
}
